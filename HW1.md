# تمرین شماره 1

**موعد تحویل:** ساعت 23 روز جمعه 25 اردیبهشت ماه 1405

## موضوع
سیستم‌های توصیه‌گر مبتنی بر محبوبیت و سیستم‌های پالایش مشارکتی مبتنی بر حافظه

در این تمرین با استفاده از مجموعه‌داده‌ی MovieLens که به پیوست ارسال شده است، چهار سیستم توصیه‌گر را پیاده‌سازی نموده و به ارزیابی و مقایسه‌ی عملکرد آن‌ها می‌پردازید.  
خروجی نهایی باید شامل فایل(های) کد و گزارش متنی شامل نتایج ارزیابی‌ها در یک جدول و تحلیل نتایج باشد.

توصیه می‌شود تا حد امکان از طراحی ماژولار بهره‌مند شوید تا به بازنویسی کدها به ازای مقادیر مختلف پارامترها نیازمند نباشید.

## پیش‌پردازش داده‌ها

1. داده‌های `rating` را بر اساس زمان (`timestamp`) مرتب نموده و با نسبت 80 به 20، به دو بخش آموزش و آزمون تقسیم کنید.
2. در داده‌ی آزمون فقط رکوردهای مربوط به کاربرانی را نگه دارید که برای آن‌ها در بخش آموزش، حداقل یک امتیاز (`rate`) وجود دارد.  
   به عبارت دیگر، کاربری که بعد از آستانه‌ی زمانی شروع به امتیازدهی کرده باشد، سابقه‌ای از تعاملاتش در مجموعه‌ی آموزش وجود نداشته و باید از دادگان آزمون حذف شود.  
   پس از اعمال این فیلتر، تعداد امتیازها، کاربرها و آیتم‌ها را در هر یک از مجموعه‌های آموزش و آزمون گزارش کنید.
3. با ثابت در نظر گرفتن مجموعه آیتم‌ها، ماتریس امتیازها (`Rating matrix`) را برای داده‌های آموزش و آزمون ایجاد کنید.

## سیستم 1: توصیه‌گر مبتنی بر محبوبیت

1. بر اساس داده‌های آموزش، تعداد \( n \in \{1, 5, 10, 15\} \) محبوب‌ترین فیلم‌ها را استخراج کنید.  
   در گزارش خود بیان کنید که محبوبیت را چگونه تعریف کرده و از کدام روش برای اندازه‌گیری محبوبیت استفاده نموده‌اید.
2. در مجموعه‌داده‌ی آزمون، تعداد \( n \in \{1, 5, 10, 15\} \) فیلم محبوب هر کاربر را براساس امتیازهایی که به فیلم‌ها اختصاص داده است شناسایی نمایید.
3. معیارهای `HitRate@n` و `Precision@n` را براساس هم‌پوشانی بین مجموعه‌ی محبوب‌ترین فیلم‌ها و مجموعه‌ی فیلم‌های محبوب هر کاربر محاسبه نموده و در پایان، میانگین معیارهای مذکور برای همه‌ی کاربران را گزارش کنید.

## سیستم 2: توصیه‌گر کاربرمحور (پالایش مشارکتی)

1. با استفاده از مجموعه‌ی آموزش و ضریب همبستگی Pearson، برای هر یک از کاربران، گروه کاربران همترازش شامل تعداد \( k \in \{2, 5, 10\} \) کاربر مشابه را شناسایی نمایید.
2. برای هر کاربر در مجموعه‌داده‌ی آزمون، امتیاز هر آیتم را با استفاده از امتیازهای همان آیتم در بین گروه کاربران همتراز، تخمین بزنید.  
   توجه نمایید، برای تخمین امتیاز فقط از امتیازهای موجود در دادگان آموزش استفاده کنید.  
   **نکته:** محاسبه‌ی امتیاز برای فیلم‌هایی که کاربر به آن‌ها امتیازی نداده است الزامی نیست.
3. براساس امتیازهای تخمین‌زده‌شده برای هر کاربر، تعداد \( n \in \{1, 5, 10, 15\} \) فیلم برتر را جهت توصیه به وی انتخاب نمایید.
4. براساس امتیازهای کاربر در دادگان آزمون، تعداد \( n \in \{1, 5, 10, 15\} \) فیلم برتر از نظر وی را انتخاب نمایید.
5. معیارهای `HitRate@n` و `Precision@n` را براساس این دو مجموعه محاسبه نموده و در پایان، میانگین معیارهای مذکور برای همه‌ی کاربران را گزارش کنید.

## سیستم 3: توصیه‌گر آیتم‌محور (پالایش مشارکتی)

1. با استفاده از مجموعه‌ی آموزش و معیار `AdjustedCosine`، برای هر یک از فیلم‌ها، گروه فیلم‌های همترازش شامل تعداد \( k \in \{2, 5, 10\} \) فیلم مشابه را شناسایی نمایید.
2. برای هر کاربر در مجموعه‌داده‌ی آزمون، امتیاز هر آیتم را با استفاده از امتیازهای همان کاربر به آیتم‌های همتراز، تخمین بزنید.  
   توجه نمایید، برای تخمین امتیاز فقط از امتیازهای موجود در دادگان آموزش استفاده کنید.
3. براساس امتیازهای تخمین‌زده‌شده برای هر کاربر، تعداد \( n \in \{1, 5, 10, 15\} \) فیلم برتر را جهت توصیه به وی انتخاب نمایید.
4. براساس امتیازهای کاربر در دادگان آزمون، تعداد \( n \in \{1, 5, 10, 15\} \) فیلم برتر از نظر وی را انتخاب نمایید.
5. معیارهای `HitRate@n` و `Precision@n` را براساس این دو مجموعه محاسبه نموده و در پایان، میانگین معیارهای مذکور برای همه‌ی کاربران را گزارش کنید.

## سیستم 4: توصیه‌گر مبتنی بر SLIM

1. مدل SLIM را با استفاده از مجموعه‌ی داده‌ی آموزش، آموزش دهید.
2. از مدل مذکور برای پیش‌بینی امتیاز کاربران آزمون به فیلم‌ها استفاده کنید.
3. براساس امتیازهای تخمین‌زده‌شده برای هر کاربر، تعداد \( n \in \{1, 5, 10, 15\} \) فیلم برتر را جهت توصیه به وی انتخاب نمایید.
4. براساس امتیازهای کاربر در دادگان آزمون، تعداد \( n \in \{1, 5, 10, 15\} \) فیلم برتر از نظر وی را انتخاب نمایید.
5. معیارهای `HitRate@n` و `Precision@n` را براساس این دو مجموعه محاسبه نموده و در پایان، میانگین معیارهای مذکور برای همه‌ی کاربران را گزارش کنید.

## جمع‌بندی

عملکرد چهار سیستم مذکور را با یکدیگر مقایسه نموده و تحلیلی پیرامون آن ارائه دهید.

## نکات

- مدیریت حل مسئله در مواردی که گروه همترازی وجود ندارد یا هیچ‌یک از اعضای گروه همتراز امتیاز ندارند به اختیار شما است.  
  اما حتماً در گزارش ذکر کنید که چگونه این موارد را مدیریت کرده‌اید.
- از `mean-centered ratings` برای تخمین امتیازها استفاده نمایید.

موفق و پیروز باشید